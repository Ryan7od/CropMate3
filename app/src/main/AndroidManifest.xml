<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CropMate"
        tools:targetApi="31">
        <activity
            android:name=".fieldManagement.FieldManagementActivity"
            android:exported="true"
            android:label="@string/title_activity_field_management"
            android:theme="@style/Theme.CropMate" />
        <activity
            android:name=".InventoryPage"
            android:exported="true"
            android:label="@string/title_activity_inventory_page"
            android:theme="@style/Theme.CropMate" />
        <activity
            android:name=".ShowEvent"
            android:exported="false"
            android:label="@string/title_activity_show_event"
            android:theme="@style/Theme.CropMate" />
        <activity
            android:name=".ToDo"
            android:exported="true"
            android:label="@string/title_activity_to_do"
            android:theme="@style/Theme.CropMate" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>